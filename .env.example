# Required for AI features (itinerary, discover, images, voice, embeddings)
OPENAI_API_KEY=sk-your-key-here

# Optional: delete trips older than this many days (default 14). Used by: npm run cleanup
TRIP_TTL_DAYS=14
